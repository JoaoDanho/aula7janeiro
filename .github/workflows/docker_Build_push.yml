name: Build and Push Docker Image to ACR

on:
  push:
    branches: [ main ]  # ou qualquer branch/evento que quiseres

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:name: Build and Push Docker Image to ACR

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🔍 PASSO DE DEBUG (TEMPORÁRIO)
      - name: Debug secrets (TEMP)
        run: |
          echo "Registry: ${{ secrets.AZURE }}"
          echo "Username: ${{ secrets.USER }}"
          echo "Password length: ${#SECRET_PASS}"
        env:
          SECRET_PASS: ${{ secrets.PASS }}

      # 🔑 Login no registry
      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.AZURE }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASS }}

      # 🐳 Build e push
      - name: Build and push Docker image
        run: |
          IMAGE="${{ secrets.AZURE }}/myapp:${{ github.sha }}"
          docker build -t $IMAGE .
          docker push $IMAGE
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.AZURE }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASS }}

      - name: Build and push Docker image
        run: |
          IMAGE="${{ secrets.AZURE }}/myapp:${{ github.sha }}"
          docker build -t $IMAGE .
          docker push $IMAGE
